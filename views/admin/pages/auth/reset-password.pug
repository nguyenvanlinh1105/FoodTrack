extends ../../layouts/auth.pug
block content
  .card-header
    h3 Xác nhận OTP
    .d-flex.justify-content-end.social_icon
      span
        i.fab.fa-facebook-square
      span
        i.fab.fa-google-plus-square
      span
        i.fab.fa-twitter-square
  .card-body
    form( action=`/${prefixAdmin}/password/reset`
          method="POST"
          onsubmit="enableEmailInput()"
        )
      .input-group.form-group
        .input-group-prepend
          span.input-group-text
            i.fas.fa-envelope
        input.form-control(
          type='email'  
          id="email"
          name="email"
          required 
          placeholder='Email'
          disabled
          value=email
        )
      .input-group.form-group
        .input-group-prepend
          span.input-group-text
            i.fas.fa-key
        input.form-control(
          type='password'  
          id="password"
          name="newPassword"
          required 
          placeholder='Mật khẩu mới'
        )
      .input-group.form-group
        div(class="g-recaptcha" data-sitekey=site_key)
      .form-group
        input.btn.float-right.login_btn(type='submit', value='Xác nhận mật khẩu mới')
  | &#x9;&#x9;&#x9;